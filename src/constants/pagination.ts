/**
 * paginationFields is an array that defines the valid fields for pagination options.
 * It specifies the allowed query parameters for pagination, including page, limit, sortBy, and sortOrder.
 * These fields can be used to control the pagination behavior when retrieving data from the database.
 */
export const paginationFields = ['page', 'limit', 'sortBy', 'sortOrder'];
